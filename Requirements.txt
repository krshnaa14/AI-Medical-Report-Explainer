streamlit==1.26.0
PyPDF2==3.1.1
pytesseract==0.3.10
Pillow==10.1.0
groq==0.8.2
sentence-transformers==2.2.2
faiss-cpu==1.7.4
pdf2image==1.16.3
opencv-python==4.8.1.78
numpy==1.26.0


Notes:

1) faiss-cpu is used for vector search. If your system has a GPU, you could use faiss-gpu instead.
2) pytesseract requires Tesseract OCR installed locally, which README already explains.
3) You dont need to add PyTorch manually; sentence-transformers will install it as a dependency.